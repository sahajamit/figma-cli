# figma-cli

A lightweight CLI for Figma's REST API — built as a faster, leaner alternative to MCP servers for letting AI agents interact with Figma designs.

Same pattern as [atlassian-cli](../atlassian-cli/) — replace heavy MCP tool schemas with direct CLI commands and agent skill files.

## Status

Research phase. See `RESEARCH.md` for full analysis.
